<?xml version="1.0" encoding="utf-8"?>
<!-- PHASE_5: the 2x1 Mawaqit widget spec (appwidget-provider).
     updatePeriodMillis: Android's own 30-min refresh — a third safety net
     alongside WorkManager's 15-min heartbeat and the prayer-pass one-shot.
     (System treats values below 30 min as 30 min.) -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:description="@string/widget_description" />
